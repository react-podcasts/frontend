import React from 'react';
import Search from '../search';
import './header.css';

const Header = () => {
  return (
    <header className="header">
      <Search />
    </header>
  );
};

export default Header;
